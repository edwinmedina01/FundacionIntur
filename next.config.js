/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // experimental: {
    //     middleware: true,  // Asegúrate de habilitar el soporte para middleware
    // },
   
};

module.exports = nextConfig;
